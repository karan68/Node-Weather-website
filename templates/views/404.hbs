<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="css/style.css">
        </head>
        <body>
             <div class="main-content">
                 {{!-- partial header rendering --}}
            {{>header}}

            <h2> {{message}}</h2>
             </div>
             

            {{>footer}}
        </body>
</html>