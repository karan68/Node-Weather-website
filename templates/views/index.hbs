<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="css/style.css">
        </head>
        <body>
            <div class="main-content">

                {{!-- partial header rendering --}}
            {{>header}}


           {{!-- <h1>{{title}}</h1>   taking the reference from the app.js by passing title there --}}
            <img src="./img/robot.png"  />
            {{!-- <h3>By : {{name}}</h3> --}}
            </div>
          <h2>Search your city weather here !!</h2> 
    <form>
        <input type="text" name="location" placeholder="Enter location name" />
        <button>Search</button>
    </form>
    <p id="message-1"></p>
    <p id="message-2"></p>
            {{>footer}}
            <script src="./js/app.js"></script>
        </body>
</html>
